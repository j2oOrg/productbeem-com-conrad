apiVersion: apps/v1
kind: Deployment
metadata:
  name: productbeem-com-ppc-social
  labels:
    app: productbeem-com-ppc-social
spec:
  replicas: 1
  selector:
    matchLabels:
      app: productbeem-com-ppc-social
  template:
    metadata:
      labels:
        app: productbeem-com-ppc-social
    spec:
      containers:
        - name: productbeem-com-ppc-social
          image: IMAGE_PLACEHOLDER
          ports:
            - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: productbeem-com-ppc-social
  labels:
    app: productbeem-com-ppc-social
spec:
  type: LoadBalancer
  selector:
    app: productbeem-com-ppc-social
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 80
  loadBalancerIP: LOAD_BALANCER_IP_PLACEHOLDER
